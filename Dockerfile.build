FROM library/alt:p9
RUN rm -rf /etc/apt/sources.list.d/* /etc/apt/sources.list \
    && echo \
      'rpm [p9] http://download.basealt.ru/pub/distributions/ALTLinux p9/branch/x86_64 classic' >> \
      /etc/apt/sources.list \
    && echo \
      'rpm [p9] http://download.basealt.ru/pub/distributions/ALTLinux p9/branch/noarch classic' >> \
      /etc/apt/sources.list \
    && apt-get update \
    && apt-get dist-upgrade -y \
    && apt-get install -y systemd \
    && systemctl mask var-run.mount \
    && rm -rfv /var/run \
    && ln -sr ../run /var \
    && apt-get install -y glibc-locales su sudo docker-ce apt-repo \
    && echo 'Yes, do as I say!' | apt-get install --force-yes systemd-sysvinit \
    && apt-get clean \
    && find /var/lib/apt/lists/ -type f -exec rm "{}" \; \
    && rm -rf /var/cache/apt/*.bin \
    && control sudo public

VOLUME [ "/run" ]
ENTRYPOINT [ "/sbin/init" ]
